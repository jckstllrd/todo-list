export default habit = () => {
  let title;
  let desc;
  let type;
  const createHabit = (title, desc, type) => {
    this.title = title;
    this.desc = desc;
    this.type = type;
  }
  const display

  return {createHabit}
}

export {habit}